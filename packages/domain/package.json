{
  "name": "@repo/domain",
  "version": "0.0.1",
  "description": "Shared Kernel - Pure TypeScript types and error codes for Dash Finance",
  "private": true,
  "type": "module",
  "exports": {
    "./types": "./src/types/index.ts",
    "./errors": "./src/errors/index.ts"
  },
  "scripts": {
    "lint": "eslint src --ext .ts",
    "check-types": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
